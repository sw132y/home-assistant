
- id: chillout_room_mode_movie
  alias: Chillout room - Movie mode
  trigger:
    - platform: event
      event_type: telegram_callback
      event_data:
        data: '/chillout_room_mode_movie'
  action:
    - service: light.turn_on
      data:
        entity_id: light.chillroom_lights
        brightness: 30
    - service: telegram_bot.answer_callback_query
      data_template:
        callback_query_id: '{{ trigger.event.data.id }}'
        message: 'Tryb filmowy'

- id: chillout_room_mode_evening
  alias: Chillout room - Evening mode
  trigger:
    - platform: event
      event_type: telegram_callback
      event_data:
        data: '/chillout_room_mode_evening'
    - platform: time
      at: "20:00:00"
  action:
    - service: light.turn_on
      data:
        entity_id: light.chillroom_lights
        brightness: 80
    - service: telegram_bot.answer_callback_query
      data_template:
        callback_query_id: '{{ trigger.event.data.id }}'
        message: 'Tryb wieczorowy'

- id: chillout_room_mode_normal
  alias: Chillout room - Normal mode
  trigger:
    - platform: event
      event_type: telegram_callback
      event_data:
        data: '/chillout_room_mode_normal'
    - platform: time
      at: "06:00:00"
  action:
    - service: light.turn_on
      data:
        entity_id: light.chillroom_lights
        brightness: 150
    - service: telegram_bot.answer_callback_query
      data_template:
        callback_query_id: '{{ trigger.event.data.id }}'
        message: 'Tryb dzienny'
        
- id: chillout_room_mode_full
  alias: Chillout room - Full mode
  hide_entity: true
  trigger:
    platform: event
    event_type: telegram_callback
    event_data:
      data: '/chillout_room_mode_full'
  action:
    - service: light.turn_on
      data:
        entity_id: light.chillroom_lights
        brightness: 255
    - service: telegram_bot.answer_callback_query
      data_template:
        callback_query_id: '{{ trigger.event.data.id }}'
        message: 'Tryb max'